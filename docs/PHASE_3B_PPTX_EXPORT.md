# Phase 3B PPTX Export

## Goal

Phase 3B adds a real `.pptx` export path for DocuMuse. The export is generated from existing local document JSON only. It does not call an LLM, generate images, create audio, use embeddings, or access external services.

## Dependency

PPTX generation uses:

```text
pptxgenjs
```

## API

Existing export route now supports:

```text
GET /api/documents/[id]/export?format=pptx
```

Successful responses return:

```text
Content-Type: application/vnd.openxmlformats-officedocument.presentationml.presentation
Content-Disposition: attachment; filename="documuse-xxx.pptx"
```

Missing documents return JSON `404`. Unsupported formats return JSON `400`.

## Generated Slides

The basic PPTX export includes:

1. Title slide with document title, filename, export time, provider, model, and `Generated by DocuMuse`.
2. Summary slide with one-sentence summary and full summary.
3. Key points slide.
4. Keywords slide.
5. Section analysis slides.
6. PPT outline slides.
7. Creative outputs slide with podcast excerpt and image prompt text only.
8. Document Q&A slides with recent question-answer rounds and short source quotes.
9. Closing slide.

## Visual Style

- 16:9 widescreen.
- White background.
- Blue section titles.
- Simple, bright layout.
- No external fonts.
- No external images.
- No animations.
- Long text is truncated to avoid overfilled slides.

## Security Filtering

The PPTX export does not include:

- API keys.
- Full prompts.
- Raw model output.
- Full `document.text`.
- `analysisDiagnostics.rawPreview`.
- `data/settings`.
- Absolute upload paths.
- Full chunk text.

The PPTX may include:

- Document filename and title.
- Provider and model names.
- Summaries and key points.
- Section summaries.
- Existing PPT outline.
- Existing podcast script excerpt.
- Existing image prompt text.
- Short chat Q&A excerpts and source quotes.

## Current Limits

- No complex template system.
- No image generation.
- No speaker notes.
- No animations or transitions.
- No full source text export.
- Layout is intentionally simple and conservative.

## Future Work

Phase 3B.1 can add a template system, brand themes, richer slide layouts, and optional user-selected sections.
